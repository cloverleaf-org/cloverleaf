---
// Slot-only: <Eyebrow>§ 4 · Architecture</Eyebrow>
---
<div class="eyebrow"><slot /></div>

<style>
  .eyebrow {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 600;
    color: var(--color-accent);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: var(--space-3);
  }
</style>
