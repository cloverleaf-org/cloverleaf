{
  "$comment": "Two tasks that share a file in scope.files_touched, demonstrating cross-task file-overlap tracking.",
  "tasks": [
    {
      "id": "ACME-300",
      "type": "task",
      "status": "pending",
      "owner": { "kind": "agent", "id": "implementer" },
      "project": "ACME",
      "parent": { "project": "ACME", "id": "ACME-102" },
      "title": "Refactor CLI entrypoint",
      "context": {
        "rfc": { "project": "ACME", "id": "ACME-100" }
      },
      "definition_of_done": [
        "CLI entrypoint is refactored to use the new argument parser."
      ],
      "acceptance_criteria": [
        "Running `cli --help` outputs the updated help text."
      ],
      "risk_class": "low",
      "scope": {
        "files_touched": [
          "reference-impl/lib/cli.ts",
          "reference-impl/lib/args.ts"
        ]
      }
    },
    {
      "id": "ACME-301",
      "type": "task",
      "status": "pending",
      "owner": { "kind": "agent", "id": "implementer" },
      "project": "ACME",
      "parent": { "project": "ACME", "id": "ACME-102" },
      "title": "Add --verbose flag to CLI",
      "context": {
        "rfc": { "project": "ACME", "id": "ACME-100" }
      },
      "definition_of_done": [
        "CLI accepts --verbose flag and emits debug output."
      ],
      "acceptance_criteria": [
        "Running `cli --verbose` emits debug-level log lines."
      ],
      "risk_class": "low",
      "scope": {
        "files_touched": [
          "reference-impl/lib/cli.ts",
          "reference-impl/lib/logger.ts"
        ]
      }
    }
  ]
}
