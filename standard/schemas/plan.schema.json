{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cloverleaf.dev/schemas/plan.schema.json",
  "title": "Plan",
  "description": "Output of Plan & Task Breakdown: finalized RFC + Task DAG + Tasks + path→reviewer mappings.",
  "type": "object",
  "required": ["id", "type", "status", "owner", "parent_rfc", "task_dag", "tasks"],
  "properties": {
    "id": { "$ref": "https://cloverleaf.dev/schemas/work-item.schema.json#/$defs/workItemId" },
    "type": { "const": "plan" },
    "status": {
      "type": "string",
      "enum": ["drafting", "gate-pending", "approved", "rejected"]
    },
    "owner": { "$ref": "https://cloverleaf.dev/schemas/work-item.schema.json#/$defs/owner" },
    "parent": { "$ref": "https://cloverleaf.dev/schemas/work-item.schema.json#/$defs/workItemIdOrNull" },
    "dependencies": { "$ref": "https://cloverleaf.dev/schemas/work-item.schema.json#/$defs/dependencyList" },
    "parent_rfc": {
      "description": "The RFC id this Plan implements.",
      "$ref": "https://cloverleaf.dev/schemas/work-item.schema.json#/$defs/workItemId"
    },
    "task_dag": { "$ref": "https://cloverleaf.dev/schemas/dependency-dag.schema.json" },
    "tasks": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "https://cloverleaf.dev/schemas/task.schema.json" },
      "description": "Tasks emitted by this Plan. Each conforms to the Task schema."
    },
    "path_reviewer_map": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["pattern", "role"],
        "properties": {
          "pattern": { "type": "string", "minLength": 1 },
          "role": { "type": "string", "minLength": 1 }
        },
        "additionalProperties": false
      },
      "description": "Path-glob → reviewer-role mappings emitted from path analysis (suggestions only)."
    },
    "extensions": { "type": "object", "additionalProperties": true }
  },
  "additionalProperties": false
}
