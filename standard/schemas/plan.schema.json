{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cloverleaf.example/schemas/plan.schema.json",
  "title": "Plan",
  "description": "Output of Plan & Task Breakdown: finalized RFC + Task DAG + Tasks + path→reviewer mappings.",
  "$comment": "Runtime invariant (not enforceable by schema): the set of tasks[].id must equal the set of workItemRefs in task_dag.nodes. Tools should validate this separately (validator #2).",
  "type": "object",
  "required": ["id", "type", "status", "owner", "project", "parent_rfc", "task_dag", "tasks"],
  "properties": {
    "id": { "$ref": "https://cloverleaf.example/schemas/work-item.schema.json#/$defs/workItemId" },
    "type": { "const": "plan" },
    "status": {
      "type": "string",
      "enum": ["drafting", "gate-pending", "approved", "rejected"]
    },
    "owner": { "$ref": "https://cloverleaf.example/schemas/work-item.schema.json#/$defs/owner" },
    "project": { "type": "string", "minLength": 1 },
    "parent": { "$ref": "https://cloverleaf.example/schemas/work-item.schema.json#/$defs/workItemRefOrNull" },
    "relationships": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "object",
        "required": ["type", "target"],
        "properties": {
          "type": { "$ref": "https://cloverleaf.example/schemas/work-item.schema.json#/$defs/relationshipType" },
          "target": { "$ref": "https://cloverleaf.example/schemas/work-item.schema.json#/$defs/workItemRef" }
        },
        "additionalProperties": false
      }
    },
    "parent_rfc": {
      "description": "The RFC this Plan implements.",
      "$ref": "https://cloverleaf.example/schemas/work-item.schema.json#/$defs/workItemRef"
    },
    "task_dag": { "$ref": "https://cloverleaf.example/schemas/dependency-dag.schema.json" },
    "tasks": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "https://cloverleaf.example/schemas/task.schema.json" },
      "description": "Tasks emitted by this Plan. Each conforms to the Task schema."
    },
    "path_reviewer_map": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "object",
        "required": ["pattern", "role"],
        "properties": {
          "pattern": { "type": "string", "minLength": 1 },
          "role": { "type": "string", "minLength": 1 }
        },
        "additionalProperties": false
      },
      "description": "Path-glob → reviewer-role mappings emitted from path analysis (suggestions only)."
    },
    "extensions": { "$ref": "https://cloverleaf.example/schemas/extensions.schema.json" }
  },
  "additionalProperties": false
}
