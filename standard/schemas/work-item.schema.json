{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cloverleaf.dev/schemas/work-item.schema.json",
  "title": "WorkItem",
  "description": "Abstract parent of all Cloverleaf trackable units (RFC, Spike, Plan, Task).",
  "$comment": "Permissive at the abstract level (additionalProperties: true); concrete types (rfc/spike/plan/task) tighten via additionalProperties: false. Reusable component definitions live under $defs and are $ref'd by per-type schemas.",
  "type": "object",
  "required": ["id", "type", "status", "owner"],
  "properties": {
    "id": { "$ref": "#/$defs/workItemId" },
    "type": {
      "type": "string",
      "enum": ["rfc", "spike", "plan", "task"]
    },
    "status": {
      "type": "string",
      "minLength": 1,
      "description": "Lifecycle status. The set of valid statuses depends on the concrete type and is defined in per-type schemas."
    },
    "owner": { "$ref": "#/$defs/owner" },
    "parent": { "$ref": "#/$defs/workItemIdOrNull" },
    "dependencies": { "$ref": "#/$defs/dependencyList" },
    "extensions": { "$ref": "https://cloverleaf.dev/schemas/extensions.schema.json" }
  },
  "additionalProperties": true,
  "$defs": {
    "workItemId": {
      "type": "string",
      "minLength": 1,
      "description": "A non-empty Work Item id."
    },
    "workItemIdOrNull": {
      "description": "A Work Item id, or null when no parent applies.",
      "anyOf": [
        { "$ref": "#/$defs/workItemId" },
        { "type": "null" }
      ]
    },
    "dependencyList": {
      "type": "array",
      "items": { "$ref": "#/$defs/workItemId" },
      "uniqueItems": true,
      "description": "Work Item ids this item depends on. Must be unique."
    },
    "owner": {
      "type": "object",
      "description": "Identifies who owns a Work Item — a human or an agent.",
      "required": ["kind", "id"],
      "properties": {
        "kind": { "type": "string", "enum": ["human", "agent"] },
        "id": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": false
    }
  }
}
